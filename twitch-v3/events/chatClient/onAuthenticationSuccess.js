import functionsFunc from '../../functions/index.js';
let functions = functionsFunc();

// channel, user, text, msg

export default async function(client) {

    console.log(`${client.channel} : CONNECTED`);

};